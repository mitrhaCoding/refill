name: Trigger auto deployment for liquid-sort-game-capp

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/liquid-sort-game-capp-AutoDeployTrigger-eac8bf1b-2fbc-49d9-a083-5ab48a0706b4.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.LIQUIDSORTGAMECAPP_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.LIQUIDSORTGAMECAPP_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.LIQUIDSORTGAMECAPP_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: 
          registryUsername: ${{ secrets.LIQUIDSORTGAMECAPP_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.LIQUIDSORTGAMECAPP_REGISTRY_PASSWORD }}
          containerAppName: liquid-sort-game-capp
          resourceGroup: poland-second-rg
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


